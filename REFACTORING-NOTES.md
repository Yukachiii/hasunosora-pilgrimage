# コード整理メモ（削除・統合候補）

最終確認日: 2026-09-11
確認時点: `7fe4e42` 以降のUIテスト版機能移植作業中

このメモは、今後行うリファクタリングで「何を、どのタイミングで削れるか」を人が判断するためのものです。
現時点では、ここに書いたコードやファイルは削除していません。

## 先に結論

いちばん大きな重複は、現在の公開画面と新しいUIテスト版が別々のアプリとして存在していることです。
新デザインを正式採用したあと、旧画面と試作用の入口を順番に削除すれば、かなり整理できます。

ただし、先に試作ファイルを消すと新画面そのものが動かなくなります。次の順番を守ります。

1. 新しいUIを本番用の共通コードへ移す
2. 本番URLで各機能が動くことを確認する
3. 旧画面の同じ機能を削除する
4. 最後にテスト専用の入口と残ったCSSを削除する

今回の機械チェックでは、TypeScript側に「定義しただけで一度も使っていない関数や変数」は見つかりませんでした。
そのため、今すぐ雑に消せる処理はほぼありません。主な整理対象は、役割が重複したページと、画面から参照されなくなったCSSです。

## 正式版への切替後に削除するもの

### 1. 最初のガイド試作ページ

対象:

- `github-pages/guide-test/` 以下の4ファイル
- `vite.pages.config.ts` の `guideTest` というビルド設定
- `tests/rendered-html.test.mjs` のガイド試作専用チェック

理由:

このページはガイド画面だけを確認するために作った最初の試作です。現在の `ui-test` にはガイドを含む公開画面一式があるため、役割が重複しています。

削除する条件:

- 新しい本番ガイドに「訪れるときのお願い」が残っている
- ガイド画像の拡大表示が動く
- PCとスマートフォンの両方で表示確認が終わっている

### 2. UIテスト版だけの入口

対象:

- `github-pages/ui-test/index.html`
- `github-pages/ui-test/main.tsx`
- `vite.pages.config.ts` の `uiTest` というビルド設定
- `tests/rendered-html.test.mjs` のUIテスト版だけを対象にしたチェック

理由:

正式採用後は、通常の公開URLが新デザインを表示します。その時点で `/ui-test/` という別の入口は不要になります。

注意:

`UiTrialApp.tsx`、`use-live-planner.ts`、`ui-test.css` は、先に内容を本番用コードへ移します。入口と一緒に先に消してはいけません。

UIテスト版は現在、次の機能を本番候補として動かせる状態にしています。

- 地図・スポット・カード・コラボの切替、検索、絞り込み
- 写真とカード画像の拡大表示、写真クレジット、カード著作権表示
- 複数日の予定、訪問順のドラッグ、滞在時間、宿泊地、時間固定の予定
- Mapbox経路、公共交通の区間確認、当日の進捗と時刻調整
- 日程を切り替えたときの計算済み経路の再利用
- 共有URLのプレビューと予定の取込み
- 写真・スポット投稿フォーム

予定は `hasunosora-pilgrimage.ui-test-planner.v1` に保存し、本番予定とは分離しています。投稿も `/api/ui-test-submissions` から専用の `ui-test` フォルダーへ保存します。本番切替前に、この分離処理を誤って削除しないでください。

2026-09-10の画面確認では、カード画像の拡大画面が一覧の背面に隠れる問題と、新しい日程を追加した際に前日の経路キャッシュまで消える問題を修正しました。どちらも見た目だけの確認や単一画面のテストでは見落としやすいため、本番移行後も「一覧から画像を開く」「複数の日程を往復する」という一連の操作で確認します。

2026-09-11のスマートフォン調整は、既存ルールを壊さず比較できるよう `ui-test.css` の末尾に上書きルールをまとめています。正式版へ移す際は、同じセレクターの古いモバイルルールと末尾の上書きを一つに統合し、上書かれて使われなくなった宣言を削除します。特に、トップ画像、一覧フィルター、予定編集欄、各種モーダルの指定が重複しています。見た目の確認前に末尾だけを削除すると今回の修正も消えるため、統合後に削除してください。

また、訪問順のドラッグは「操作中の仮順序」と「指を離した後の確定順序」を分けています。操作中に本体の予定や経路計算を更新しないための分離なので、共通化するときも一つの状態へ戻さないでください。

### 3. 旧公開画面の重複部分

主な対象:

- `app/PilgrimageApp.tsx` 内の、移行済み画面と同じ役割を持つ表示部分
- `app/globals.css` 内の、移行済み画面専用の装飾

現在の規模:

- `PilgrimageApp.tsx`: 約3,650行
- `globals.css`: 約9,390行

理由:

どちらも機能追加のたびに同じファイルへ追記されており、探す・予定・当日・ガイド・共有のコードが一か所に集まっています。新画面へ切り替えたあとも旧表示を残すと、同じ修正を二か所へ行う状態が続きます。

削除方法:

一括削除はせず、「探す」「予定」「当日」「ガイド」「共有」の順に、新画面が動くことを確認してから旧部分を一つずつ削除します。

## 今でも未使用と判断できるCSS候補

`github-pages/ui-test/ui-test.css` には、画面の作り直し前に使っていた装飾が残っています。画面側のコードから参照されていないことを機械的に確認した候補です。

### 旧検索・絞り込み表示

- `.ui-trial__search-row`
- `.ui-trial__filter-toggle`
- `.ui-trial__search-filters`
- これらだけが使っている `ui-trial-filters-in` アニメーション

現在はモーダル内の `.ui-trial__explore-window-filters` を使っています。

### 本物の地図へ置き換える前の簡易地図

- `.ui-trial__map-grid`
- `.ui-trial__map-river`
- `.ui-trial__map-pin`
- `.ui-trial__map-pin--1` から `.ui-trial__map-pin--4`

現在は共通のMapbox地図を使っています。

### 以前の探す画面の構成

- `.ui-trial__explorer`
- `.ui-trial__map-selection`
- `.ui-trial__explore-map`
- `.ui-trial__collaboration`
- `.ui-trial__collaboration-heading`
- `.ui-trial__feedback`

注意:

`.ui-trial__collaboration-options` と `.ui-trial__collaboration-summary` は現在も使っています。名前が似ていますが、こちらは削除しません。

## 統合してから片方を削除するもの

### 予定・経路計算の処理

現在は、旧公開画面の `PilgrimageApp.tsx` と新しい `ui-test/use-live-planner.ts` に、予定の保存・並べ替え・経路計算を動かす処理が分かれています。

今後は本番用の予定処理を一つにまとめます。新旧両方が同じ処理を使える状態にしてから、古い方を削除します。これにより「テスト版では直ったが本番では直っていない」という食い違いを防げます。

### ヘッダー、下部メニュー、モーダル

旧公開画面、ガイド試作、UIテスト版がそれぞれ似た部品を持っています。新デザインを本番へ移すときに共通部品へまとめ、重複した表示コードとCSSを削除します。

### ガイド画像

現在の画像ファイルは、すべてどこかの画面またはテストから参照されています。そのため、今すぐ削除できる画像はありません。

次の2枚も、現在のUIテスト版のガイドで使っています。

- `public/guide/05-plan-time.png`
- `public/guide/07-card-search.png`

旧公開画面と最初のガイド試作を削除したあとも、新しいガイドに残す予定です。新しいガイド自体を作り直す場合だけ、参照を確認して削除を判断します。

## 大きいので分割するが、機能は削除しないもの

### 管理画面

`app/admin/AdminApp.tsx` は約1,800行あります。次の単位へ分けると読みやすくなります。

- 公開操作
- API使用状況
- 写真管理
- スポット管理
- バージョン管理

これはファイルを分ける整理です。管理機能そのものは削除しません。

### 投稿受付サーバーと管理サーバー

`community-server.mjs` と `server.mjs` には、JSONの返却やファイル書込みなど似た小さな処理があります。共通の部品へまとめられますが、二つのサーバーは役割が違います。

- `community-server.mjs`: インターネットから利用者投稿を受け付ける
- `server.mjs`: 自宅内の管理画面を動かす

したがって、どちらか一方を丸ごと削除する対象ではありません。

## ローカルで消しても再作成できるもの

次はビルドやテストで作られる一時的な出力です。Gitには登録されていません。

- `pages-dist/`
- `admin-dist/`
- `outputs/`
- `tsconfig.tsbuildinfo`

容量整理のために消しても、ビルド時に再作成できます。ただしコード整理の効果とは別です。

`node_modules/` も `npm.cmd ci` で再作成できますが、消すと依存関係の再インストールが必要になるため、通常のリファクタリングでは触りません。

## 削除してはいけないもの

- `content/` のスポット・カード・コラボ・写真一覧データ
- `public/photos/` と `public/card-images/` の公開画像
- `private/` の審査待ち投稿や診断記録
- `.env.example` と自宅サーバーの起動・自動更新用スクリプト
- 投稿受付、予定共有、経路計算のテスト

これらは見た目が古いコードではなく、運用データ・安全対策・復旧手順に関係しています。削除が必要になった場合は、別途目的と影響を確認します。

## 作業する順番

1. 現在の本番とUIテスト版の動作を記録する
2. 予定処理など、新旧で重複している処理を共通化する
3. 新デザインを通常の公開URLへ移す
4. 画面ごとに動作確認し、その画面の旧コードと旧CSSを削除する
5. `guide-test/` と `/ui-test/` の入口を削除する
6. 未使用CSSと不要になったガイド画像を再確認して削除する
7. PC・スマートフォンの表示、投稿、共有、経路計算、管理画面を通しで確認する

削除はこのメモの項目をまとめて一度に実行せず、一項目ごとにコミットを分けます。問題が出た場合に、その削除だけを戻せるようにするためです。
